import bmi

def test_calc_bmi():
    assert calc_bmi(monkeypatch):
    monkeypatch.setattr('builtins.input', lambda _: "1.63")
    monkeypatch.setattr('builtins.input', lambda _: "60")
    assert datatypes.input_then_add() == 22.56)